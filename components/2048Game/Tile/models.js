// import { pixelSize } from '../../../styles/globals.css';
const pixelSize = 8;
const tileMargin = 2 * pixelSize;

const tileWidthMultiplier = 12.5;

const tileWidth = tileWidthMultiplier * pixelSize;

export const tileTotalWidth = tileWidth + tileMargin;
